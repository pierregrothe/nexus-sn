id: simple-approval-flow-readiness
version: "1.0.0"
description: Pre-deploy readiness for the simple-approval-flow workflow.
applies_to:
  - simple-approval-flow
rules:
  - id: target-scope-records-exist
    description: At least one sys_scope record is captured for resolution.
    severity: ERROR
    phase: PRE_APPLY
    scope:
      kind: table
      table: sys_scope
    required_tables:
      - sys_scope
    logic: AND_ALL
    constraints:
      - operator: count_gte
        table: sys_scope
        threshold: 1
        filter: []
